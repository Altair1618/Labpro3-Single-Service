export type CompanyData = {
    id: number;
    name: string;
    address: string;
    phone: string;
    taxCode: string;
}

export type ItemData = {
    id: number;
    name: string;
    price: number;
    stock: number;
    code: string;
    producerId: number;
}
